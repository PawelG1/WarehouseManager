<UserControl
    x:Class="WarehouseManagerApp.Views.ProductsList"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WarehouseManagerApp.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Margin="10" Orientation="Horizontal">
            <ComboBox
                x:Name="selectedWarehouse"
                Margin="50,10"
                DisplayMemberPath="Name"
                ItemsSource="{Binding Warehouses}"
                SelectedValue="{Binding SelectedWarehouseId}"
                SelectedValuePath="Id" />
            <TextBlock
                Grid.Row="0"
                Margin="50,10"
                FontSize="16"
                FontWeight="Medium">
                Products Count:<Run Text="{Binding ProductsCount}" />
            </TextBlock>
        </StackPanel>

        <!--  Error Message  -->
        <Border
            Grid.Row="1"
            Margin="10,0,10,10"
            Padding="10"
            Background="#F8D7DA"
            BorderBrush="#F5C6CB"
            BorderThickness="1"
            CornerRadius="3">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasError}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock
                Foreground="#721C24"
                Text="{Binding ErrorMessage}"
                TextWrapping="Wrap" />
        </Border>

        <ListView
            Grid.Row="2"
            Margin="10"
            ItemsSource="{Binding Products}">
            <ListView.View>
                <GridView>
                    <GridViewColumn
                        Width="50"
                        DisplayMemberBinding="{Binding Id}"
                        Header="ID" />
                    <GridViewColumn
                        Width="150"
                        DisplayMemberBinding="{Binding Name}"
                        Header="Name" />
                    <GridViewColumn
                        Width="100"
                        DisplayMemberBinding="{Binding SKU}"
                        Header="SKU" />
                    <GridViewColumn
                        Width="80"
                        DisplayMemberBinding="{Binding Quantity}"
                        Header="Quantity" />
                    <GridViewColumn
                        Width="100"
                        DisplayMemberBinding="{Binding minimumQuantity}"
                        Header="Minimum Qty" />
                    <GridViewColumn
                        Width="150"
                        DisplayMemberBinding="{Binding Warehouse.Name}"
                        Header="Warehouse" />
                </GridView>
            </ListView.View>
        </ListView>

        <ProgressBar
            Grid.Row="2"
            Height="30"
            VerticalAlignment="Center"
            IsIndeterminate="True">
            <ProgressBar.Style>
                <Style TargetType="ProgressBar">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ProgressBar.Style>
        </ProgressBar>
    </Grid>
</UserControl>
